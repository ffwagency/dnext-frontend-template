export default function Loading() {
    return (
        <div className="fixed inset-0 flex items-center justify-center">
            <div className="border-gray-200 size-8 animate-spin rounded-full border-4 border-t-primary" />
        </div>
    );
}
